# APK build

This project includes `.github/workflows/build-apk.yml`.
GitHub Actions builds an installable debug APK and exposes it as the artifact `PokemonCardmarketScanner-APK`.
The APK file is `app-debug.apk`.
